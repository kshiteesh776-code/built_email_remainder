# SMTP Credentials & Configuration
# For Gmail, use an App Password generated under Google Account -> Security -> 2-Step Verification -> App passwords
EMAIL_ADDRESS=your_email@gmail.com
EMAIL_PASSWORD=your_app_password

# Optional SMTP Settings (defaults to Gmail port 587 STARTTLS)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
